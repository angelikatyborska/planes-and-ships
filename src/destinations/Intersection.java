package destinations;

import core.Coordinates;

public class Intersection extends Destination {
  public Intersection(Coordinates coordinates, int vehicleCapacity) {
    super(coordinates, vehicleCapacity);
  }
}
